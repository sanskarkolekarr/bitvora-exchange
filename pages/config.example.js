/**
 * BITVORA EXCHANGE — Frontend Configuration
 * Copy this file to config.js and fill in your values.
 * config.js is gitignored — never commit it.
 */

const BITVORA_CONFIG = {
    // Supabase public-safe values (anon key only — no service role key here)
    SUPABASE_URL: "https://your-project.supabase.co",
    SUPABASE_ANON_KEY: "your-anon-key-here",

    // Backend API URL (Cloudflare-proxied domain)
    API_BASE_URL: "https://api.bitvora.exchange",
};
