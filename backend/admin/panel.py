"""
BITVORA EXCHANGE — Admin Panel
Reserved for optional admin panel UI.
Currently, all admin operations are performed via direct API calls
using the ADMIN_SECRET_KEY as Bearer token.
"""

# This module is intentionally minimal.
# Admin operations are handled by routes/admin.py.
# A future admin web UI can be mounted here as a static app.
