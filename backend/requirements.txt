# ── Core ──────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── Async PostgreSQL (Supabase as DB) ─────────
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# ── Async Redis (queue + locks) ───────────────
redis[hiredis]>=5.2.0

# ── Async HTTP (verifier RPC + price API) ─────
aiohttp>=3.10.0
solana>=0.34.0
web3>=6.20.0
solders>=0.21.0

# ── Telegram bot (async, aiogram v3) ──────────
aiogram>=3.15.0

# ── Utilities ─────────────────────────────────
python-dotenv>=1.0.1
orjson>=3.10.0

# ── Auth ──────────────────────────────────────
passlib[bcrypt]
python-jose[cryptography]
